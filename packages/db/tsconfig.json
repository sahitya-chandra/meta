{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": ".",
    "composite": true,         // allow project references if you use them later
    "declaration": true,       // emit .d.ts files
    "declarationMap": true
  },
  "include": ["index.ts"],
  "exclude": ["node_modules", "dist"]
}
