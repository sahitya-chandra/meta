name: PR Checks

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Enable Corepack
      run: corepack enable

    - name: Install dependencies
      run: npm install

    # - name: 🔍 Type check all projects
    #   run: npm run type-check

    - name: 🧹 Lint monorepo (server & packages)
      run: npm run lint

    - name: 🧹 Lint Next.js web app
      run: npm run lint:web

    - name: 💅 Check code formatting
      run: npm run format:check

    - name: 🔧 Build packages
      run: npm run build:packages

    - name: 🏗️ Build server
      run: npm run build:server

    - name: 🌐 Build web app
      run: npm run build:web

  # Optional: Run tests if you have them
  # test:
  #   name: Run Tests
  #   runs-on: ubuntu-latest
  #   needs: code-quality
  #   
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #   
  #   - name: Setup Node.js
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version: '18'
  #       cache: 'npm'
  #   
  #   - name: Enable Corepack
  #     run: corepack enable
  #   
  #   - name: Install dependencies
  #     run: npm install
  #   
  #   - name: Run tests
  #     run: npm test